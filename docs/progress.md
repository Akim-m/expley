# Progress

Living status + roadmap for the temporal-exploit modeling system. Update as work lands.

Last updated: 2026-06-30 (in-wild EPSS-parity work + adversarial RE verification, worktree `inwild-epss-parity`).

## ►► START HERE — handoff for the next agent

- **Latest (2026-06-29/30, worktree `inwild-epss-parity`) — EPSS parity on the in-wild target.** Re-targeted the comparison to the in-wild label (the closest honest proxy for what EPSS predicts) and built a same-target walk-forward head-to-head (`scripts/inwild_epss_parity.py` → `artifacts/inwild_epss_parity.json`; writeup `docs/inwild_epss_parity_2026-06.md`; figure `docs/figures/fig_epss_parity.png`). **Result: structural beats EPSS on RANKING by a modest, significant +0.100 AUC@30 [0.055,0.145] / +0.134 AUC@90; PR-AUC TIED; EPSS wins recall@top-1%.** Caught + fixed a measurement bug — wrapping EPSS (a calibrated score) in an xgb-AFT fit collapses it to ~chance (0.501); the honest baseline ranks the **raw** EPSS percentile (0.695). Added a `score_col` passthrough to `rolling_origin_backtest`. **This corrects prior "beats EPSS-only" margins (model-wrapped EPSS was too weak a baseline).** Composite "system" arm = this + the PoC→KEV escalation (`defender_score.py` STATE 2, recall@top-10% 0.50).
  - **Adversarially RE-verified (2026-06-30, 2 agents):** the +0.100 win **reproduces to the digit, deterministically** (zero run-to-run jitter), is **leakage-free** (68 publication-safe cols, no description/`vrs_presence`/snapshot-EPSS), is **paired correctly** on shared origins with identical per-arm test frames, and is **not an EPSS-coverage artifact** — in-wild positives have 99.9% publication-time EPSS coverage and restricting both arms to real-EPSS-only rows *widens* the gap (+0.106 AUC@30). Mechanism: in-wild positives sit at median publication-time EPSS percentile **0.168** (EPSS is dynamic, hasn't reacted at t=0). **Honesty caveat folded into the docs:** the "in-wild" label is in practice **~93% VulnCheck KEV** (CISA adds 90 test events, Google 0-day adds **0** — all dropped as pre-pub negative-duration), and `date_added` is an **administrative catalog-add date, not exploitation onset** (median lag 175d; only ~22% inside the 30-day horizon). So read the target as a *publication-anchored known-exploited proxy*; the comparison is fair (both arms see the identical proxy) but the "same target as EPSS" name is the soft part, not the number. The extra VulnCheck labels tightened the CI via *n*, not crisper timing.
- **Label lever (Phase 2, gated on credentials):** **VulnCheck KEV community** is the immediate win — `GET /v3/backup/vulncheck-kev` (free `Authorization: Bearer` token, 1000/min), event date = `min(date_added, vulncheck_reported_exploitation[].date_added)`, ~+1,000–1,700 in-wild CVEs. **GreyNoise** needs the free Research-Community grant (manual approval) and only gives a ~90-day rolling CVE window → prospective-only, weak for backfill. **Shadowserver is RULED OUT** (region/registration-locked, provides nothing — user-confirmed 2026-06-29).
- **Where we are.** Penalized **Cox + EPSS features is the in-wild model** (AUC@90 ≈ 0.82, recall@top-decile ≈ 0.51, IPA ≈ 0). Evaluated by the **rolling-origin (walk-forward) backtest** (`temporal_exploit.backtest`; driver `scripts/inwild_method_headtohead.py`). For time-to-first-weaponization the headline is xgb-AFT 0.598 / cox 0.565 (PoC-dominated — see the framing caveat in CLAUDE.md).
- **The one thing to internalize.** The in-wild ceiling is **DATA-LIMITED (396 events), not model- or calibration-limited.** This is now proven by exhausting the alternatives: RSF, gradient-boosted survival, XGBoost-AFT, mixture-cure, **stacked transfer, and temperature recalibration all ≤ baseline Cox** (this session). IPA≈0 is the rare-event reality, not a bug to fix with a fancier model.
- **Highest-leverage next step (roadmap #1) — raise the ceiling with more/earlier LABELS. BOTH SOURCES ARE NOW WIRED (2026-06-19); they just need credentials:**
  - **VulnCheck KEV** (~173 % larger than CISA, ~27 days earlier) — **fully end-to-end**. Set `VULNCHECK_API_TOKEN` (free community tier) and run `temporal-exploit refresh --live-dir … [--vulncheck-token …]`; it fetches, the merge layer dedups, and `vulncheck_kev` already flows into `IN_WILD_SOURCES`. Then re-merge → rebuild → re-run the backtest.
  - **[RULED OUT 2026-06-29 — region/registration-locked, provides nothing usable; do not pursue.]** **Shadowserver** honeypot feed (`src/temporal_exploit/fetch/shadowserver.py`) — connector + `EVENT_SOURCES`/`IN_WILD_SOURCES`/merge/refresh wiring all in. Set `SHADOWSERVER_API_KEY`/`SHADOWSERVER_API_SECRET` (or `~/.shadowserver.api`) and `refresh` picks it up. **Left to do:** confirm the `report` name + cve/timestamp field names against a live key (scaffold defaults used — use the API's `{"help": true}`); the Reports API is day-scoped, so historical first-seen accumulates via repeated refreshes (merge keeps earliest) — a bulk date-loop backfill isn't written yet; optionally add a `fetch shadowserver` CLI case (refresh already covers it). No live verification yet (needs a subscription).
  - Both could 2–4× the true in-wild events. Use *first-reported-evidence date*, not catalog-add date. See `docs/research_pathways_2026-06.md` §Pathway 1.
- **Do NOT redo these documented dead-ends:** mixture-cure (non-identifiable — no KM plateau), RSF/GBM/deep nets (lose at 396 events, Burk et al. 2026), stacked transfer (source shares the target's features), any recalibration (event-starved-origin fragility). Evidence: `docs/literature_rare_event_exploit_prediction.md` + `docs/research_pathways_2026-06.md`.
- **Other ranked roadmap items (cited in `docs/research_pathways_2026-06.md`):** shared-frailty illness-death (the one *untried model* pathway with a structural reason to help — borrows PoC→in-wild via a shared frailty); XGBoost native `survival:cox` (`hist`, no O(n²)) on the *abundant* target; proper scoring rules (RCLL — our IPA basis ISBS is not strictly proper); decision-curve/net-benefit eval; sufficient-follow-up test to formally close cure.
- **Reproduce anything:** `scripts/inwild_*.py` — `method_headtohead` (cox/rsf/gbm), `stacked_transfer`, `temperature`, `feature_prune`, `subcohort_drop`. Constraints still bind: ≤6–8 GB RAM / ≤7 GB VRAM, strict point-in-time/no-leakage.

## Recently landed (this initiative)
- **New scope: causal characterization + patch-vs-exploit race (2026-06-22) — `docs/causal_and_patch_race_2026-06.md`.** After the in-wild label ceiling saturated and Direction C (aggregate forecasting) was measured-dead + Direction B (OSS population) rejected, added two synergistic scopes on the *abundant* first-weaponization target. **(1) Causal characterization** (`src/temporal_exploit/causal.py` = adjusted Cox + stabilized IPW + overlap + E-value; `tests/test_causal.py` 4✓): **wormable attributes causally accelerate weaponization** (adjusted HR 1.29, IPW 1.41, E-value 1.68; raw median 100d vs 277d; robust 1.17–1.41 across specs), unauth-network-high-impact HR 1.24; **ATT&CK-chain-mapping is a confounded null** (positivity-violated — the framework refusing an unsupported estimate the prior log-rank study missed). **(2) Patch-vs-exploit race** (NVD reference fetch 359,627 CVEs + GitHub-GraphQL fix-commit mine 20,549 CVEs/67.7% dated; `scripts/patch_race_analysis.py`): **the race is bimodal and patch-date observability is the selector** — commit-dated OSS cohort is patched a median 14d *before* disclosure (99% pre-decided; time-varying n-day Cox underpowered at ~1% identifying set), 0-days are 100% exploited-before-patch (median 9d to patch), and the **unbiased corpus-wide rate is 28.6% (first-weap) / 35.5% (in-wild) exploited on/before publication — independently matching VulnCheck's 28.96% of 2025 KEVs**. Lesson: commit-date race models are selection-biased toward already-won races; the unbiased signal needs no fetch.
- **Remaining deliverables + project-wide debug/optimize (2026-06-21) — spec `docs/superpowers/specs/2026-06-21-remaining-work-tool-calibration-beatepss-design.md`; RE via ≤2 subagents × 3–5 loops per item (user rule).**
  - **D1 downstream triage tool** — `temporal_exploit/triage.py` + `temporal-exploit triage` CLI; state-aware per-CVE action list. RE fixed a lead-time metric bug (`operating_points` reported `horizon−duration` ≈86d, true PoC→KEV lead ~4d median / p75 ~11d) + a `tiers()` no-signal flood. `docs/defender_interpretation_2026-06.md` corrected.
  - **D2 calibration at horizons** — `calibration.calibration_slope_intercept` + `scripts/calibration_by_horizon.py` (reuses existing KM-within-bin reliability + per-horizon Brier). Cox ≈ XGB-AFT discrimination (0.584/0.585) but **Cox far better calibrated** (slope ~0.6 vs ~0.2); Cox still over-predicts at long horizons. Doc `docs/calibration_by_horizon_2026-06.md`.
  - **D3 gated hill-climb (autoresearch-style)** — `temporal_exploit/hillclimb.py` + `scripts/beat_epss_hillclimb.py`. Generate-and-keep over leakage-safe feature groups; accept gate = significant paired per-origin delta + shuffle-null + plateau stop. **EPSS+CVSS significantly beats EPSS-only** at recall@top-10%@30d (0.103→0.130, Δ+0.027 CI[0.013,0.042], robust across operating points), then plateaus. RE-verified real + leakage-free. Doc `docs/beat_epss_attempt_2026-06.md`.
  - **DeepHit collapse FIXED** — root cause was time-bin **placement**, not loss `alpha` (falsified: raising alpha worsened it) nor bin count. Quantile discretization recovers CIF@90 ~3e-6→~0.17 (AJ truth ~0.10), better concordance; now the default (`scheme="quantiles"`, `num_durations=50`, `batch_size=1024`). Doc `docs/deephit_imbalance_fix_2026-06.md`. Usable, still not better than Cox.
  - **6 optimizations, all RE-verified bit-identical:** backtest earliest-event hoist (**7.4× faster** label-building), vectorized KM for calibration bootstrap (**~13s→2.6s**), vectorized `_per_subject_nll` (exact) + Breslow `H0` (O(n²)→O(n log n)), DeepHit/DeepSurv `batch_size` 256→1024 (GPU 25%→75% util), `evaluate_deephit` single `predict_cif`. Accuracy audit clean; minor edge fixes (triage EPSS no-signal guard, dead ATT&CK keys). RAM/VRAM under cap throughout.
- **Model-improvement + edge-case audit (2026-06-20) — `docs/model_improvement_and_edge_cases_2026-06-20.md` (all 13 concrete claims triple-verified by independent agents; new modules RE-audited ≥5×).** Acting on "research how to make it better + find all edge cases + verify thrice + be honest." Wins + honest corrections:
  - **The data limit is partly SELF-INFLICTED by label processing.** Two filters discard ~70% of fetched VulnCheck + **100% of Google 0-day** (132/133 are exploited pre-disclosure → negative-duration → dropped by `prepare_modeling_frame`; the in-wild model trains on ~no 0-day timing).
  - **MEASURED + IMPLEMENTED floor fix (commit d23c7b7).** The CISA `published<2021-11-03` floor is mis-applied to VulnCheck's first-evidence dates (CISA: 287 entries that day; VulnCheck: 53, smooth back to 2000). `in_wild_clock_start` now returns `None` when a broad first-evidence source (`EVIDENCE_SOURCES={vulncheck_kev,shadowserver}`) is active — VulnCheck's earliest-wins merge self-heals 234/287 CISA-spike CVEs. **AUC@90 0.808→0.827 (paired CI [+0.0012,+0.0281] excludes 0), IPA collapse fixed** (`scripts/inwild_floor_ablation.py`). google_0day excluded (too few CVEs to self-heal — an RE catch on the design).
  - **VulnCheck lift re-characterised honestly.** 396→1201 events (3×), AUC flat, recall@10% 0.51→0.54, **calibration NOT collapsed** — the IPA@90 mean −0.083 is ONE event-starved origin (2022-04: 37 fast train events vs 98 slow test events — a genuine non-stationarity miscalibration, root-caused, not a bug); median IPA ~0, paired CI includes 0.
  - **New modules (TDD + RE loops).** `temporal_recalibration.py` (Booth 2020 — refit the Cox baseline on a recent window, rank-preserving; the lit's #1 cheap fix for non-stationarity; RE caught+fixed an inf*0→NaN bug); `proper_scoring.py` (RCLL strictly-proper score, to test if IPA≈0 is a metric artifact); `decision_curve.py` (net benefit — MEASURED: the model IS useful over the 0.1–3% threshold band at 0.32% base rate, but modestly).
  - **Eval power sharpens "no model beats Cox".** The backtest detects a +0.015 AUC effect but cannot resolve IPA effects <~±0.15 → the model-class question is powered/closed, **calibration is underpowered/open** (the real frontier).
  - **CORRECTION to the roadmap: shared-frailty illness-death is a NO-GO** at current scale. The "190k abundant transitions" is state-occupancy, not transition events (only ~2,577 transition events, coupling to ~661 in-wild CVEs); in-wild EPV is now 17–21 (no longer starved); R-outside-harness breaks the only validity instrument. PU recency-reweight is the cheaper *conditional*-GO (calibration only). See the deliverable §A5.
  - **Measured: temporal recalibration is an honest NEGATIVE on in-wild** (`scripts/inwild_temporal_recal.py`): median IPA@90 0.0050→0.0039, AUC@90 identical (rank-preserving confirmed). The floor fix already subsumed it — base IPA is positive at nearly every origin and **the 2022-04 collapse went −1.17 (floored) → +0.009 (no-floor)**, so there's no staleness bias left to remove; recalibrating on the smaller recent window only adds variance. Keep the module (correct, RE-verified to 3e-16) but do NOT wire it on. The in-wild era-stress B4 is answered by the 2022-04 root-cause (37 fast train vs 98 slow test events = calibration drift, not ranking). **Left (optional):** apply RCLL with a finer survival grid; the PU recency-reweight experiment (SA4's ~30-line sketch).
  - **Strategic conclusion (enabler analyses — power + marginal-value-over-EPSS).** The defensible headline is the **t=0 cold-start in-wild head**: it beats EPSS **+0.13–0.25 AUC only at disclosure** (EPSS ~48% missing + near-chance there), and at t=0 **EPSS-at-publication HURTS** — dropping it lifts cold-start AUC 0.67→0.78 (recommend a `publication_only_no_epss` feature_view). At the landmark EPSS saturates it (`no_epss ≥ full`) — do NOT headline landmark ranking. Value prop = cold-start AUC-over-EPSS + the calibrated timing curve (IPA90>0, lead-time ~140d) EPSS can't give; model = Cox. The eval CAN certify ≥0.02 IPA / structural changes (the earlier "underpowered" was overstated — that was the outlier-dominated cross-label comparison); cheap power win = robustify the 2022-10 outlier origin (5.6× of the IPA SD). Full strategy: deliverable §F.
- **Deep-research roadmap + flow-change prototypes (2026-06-19, session 2).** Acting on "find other methods/pathways/functions to change the flow and get better results; research the latest; ≥35 papers." Six parallel literature sweeps (**60+ papers, 2022–2026**) → `docs/research_pathways_2026-06.md`. Prototyped the two most feasible weak-spot-targeting flow-changes through the backtest — both **honest negative results** that confirm the data-limited ceiling:
  - **Stacked transfer** (`augment_fn` hook + `scripts/inwild_stacked_transfer.py`): inject the abundant first-weaponization Cox's risk score as one in-wild covariate. **Δ everything ≈ 0** (AUC@90 −0.0004, IPA −0.0001 ≪ sd 0.069) — the source risk is a non-linear combination of features the in-wild Cox already has. The 396-event ceiling is also a transfer ceiling.
  - **Temperature recalibration** (`calibration.py`, `temperature=True`): 1-param S^exp(a), out-of-sample cross-fit, **provably ranking-preserving** (AUC identical 0.8173=0.8173). **Harmful on the mean** (IPA@90 −0.003→−0.092) though median-neutral — event-starved origins learn a bad temperature (same rare-event fragility as the earlier isotonic attempt). Guarded with `min_events=40`; **recommendation: recalibration OFF for in-wild.**
  - **Reverse-engineering (6 fixes):** crossfit_temperature never-abort fallback + min-events guard; clip consistency; `_merge_extra` non-numeric fail-loud; stratified source cap; non-cox temperature warning.
- **In-wild method head-to-head + rare-event literature review (2026-06-19).** Acting on "don't fixate on one method — keep checking for a better one, and research how the field handles this." Ran the rolling-origin backtest (15 quarterly origins from 2022-01-01, KEV-catalog clock 2021-11-03, 71 EPSS features, **396 in-wild test events**) across the model classes the survival literature flags as the real challengers to a penalized Cox, prospectively (identical origins, only the model varies — `scripts/inwild_method_headtohead.py`):

  | model | AUC@90 (mean / median) | sd | recall@top-10% | IPA@90 | fit time |
  |---|---|---|---|---|---|
  | **cox** | **0.817 / 0.846** | **0.069** | **0.51** | −0.003 | **78s** |
  | rsf | 0.770 / 0.775 | 0.117 | 0.42 | −0.016 | 384s |
  | gbm | 0.710 / 0.744 | 0.093 | 0.40 | −0.001 | 362s\* |
  | cure | overturned | — | actively harmful | −0.27 @180d | — |

  \* GBM (sksurv Cox-loss boosting) is **O(n²) per tree**; the full 20k-row run ran ~40 min before being killed — only tractable after capping the subsample to 4k, which then discards scarce events and tanks discrimination. **Verdict: penalized Cox is the in-wild backbone on every axis that matters** (best discrimination, lowest variance, best operational recall, 5–300× faster). This matches the published consensus — neutral tabular-survival benchmarks find *no method significantly beats Cox PH* (Burk et al. 2026), RSF *overestimates survival under imbalanced censoring*, and deep nets are non-starters at this event count. Full review with citations: **`docs/literature_rare_event_exploit_prediction.md`**.
  - **Over-parameterization probe (rejected).** 9.5 events-per-variable (664/70) is "over-parameterized" by the EPV literature, but hard-pruning to the 12 dense signals *hurt* (AUC@90 0.783 vs 0.817, **and** higher variance sd 0.113 vs 0.069) — the sparse CWE/ATT&CK/CVSS-vector one-hots carry real prospective signal (consistent with Allodi/Massacci on vuln-type risk). Ridge shrinkage already handles the EPV concern; the right remedy was penalization (in place), not feature removal.
  - **Cure model corrected** (was "the headline contribution / only model with positive IPA"). The single-split IPA win was an **identifiability artifact** — a mixture-cure fraction needs a Kaplan-Meier plateau, and our ~99.5%-censored in-wild target is *administratively* censored, not a cured population (Li/Taylor/Sy 2001). The backtest overturned it prospectively. Cox is the backbone; cure is a documented dead-end.
  - **Data-limited ceiling (the honest bottom line).** All models sit at IPA ≈ 0 on in-wild — the binding constraint is **event count (396), not model choice**, confirmed both empirically (every challenger lost) and by the EPV literature. The GPU is correctly idle: cox is CPU by design and is the best model; deep nets (the only GPU option) are disqualified at 664 events.
  - **Reverse-engineering (9 fixes, the mandatory self-audit).** (1) `fit_cox` ConvergenceError penalizer escalation (hard NaN-delta, not just the warning); (2) backtest graceful per-origin skip; (3) aggregate reports **median** + total test-event count (robust to event-starved origins); (4) RSF wired into the backtest; (5) `fit_gbm` added + wired; (6) cure-overclaim corrected with the identifiability citation; (7) **caught my own wrong claim** — the horizon-AUC subcohort restriction drops 17.7% at 180d (not "nobody"), now reported per-horizon (`horizon_auc_dropped_frac`); (8) `fit_gbm` O(n²) max_samples cap; (9) over-parameterization probe. **221 passed, 3 skipped.**
- **Wave-improvements program (2026-06-19 — plan `docs/superpowers/plans/2026-06-19-wave-improvements.md`).** Four phases, each TDD'd + committed, then a mandatory self-audit loop (≥5 fixes × 6 rounds — a standing working practice).
  - **Phase A — evaluation rigor.** `modeling.bootstrap_cindex_report` (one shared-index bootstrap → per-model percentile CI + paired deltas vs cox, on the fast truncated Harrell C; event-preserving `max_eval` subsample), `truncated_cindex` (exact under administrative censoring; = IPCW here), `paired_cindex_vs_cox` in the train report. **Honest in-wild (cutoff 2024-01-01): cox 0.837 boot-CI [0.816,0.856], cure 0.832; paired cure−cox = −0.006 CI [−0.025,+0.015] → statistically tied; xgb significantly worse (p=0).**
  - **Phase B — cure model.** Log-logistic latency (non-monotone hazard) added beside Weibull, unified analytic gradient (FD-checked both), `fit_cure(latency="auto")` AIC-selects (picks log-logistic on in-wild). `CureModel.recalibrate` = **held-out** isotonic (empirically the only one that improved test IPA: in-sample overfit to −0.12, cross-fit mismatched to −0.04). `train --cure-recalibrate`: **EPSS-enriched in-wild IPA −0.000 → +0.003** (beats the null) at a ~0.02 discrimination cost — opt-in.
  - **Phase C — NLP description features.** `nlp_features.build_description_features` (leakage-safe: freshness-gated + masked length/word-count/vuln-class keywords), `build-dataset --description-text` (loads the 112 MB description only when set). Honest mixed result: xgb 0.598→0.604, cox 0.588→0.538 — helps the tree model, hurts the linear one.
  - **Phase D — DeepHit.** `deephit.py` (lazy torch/pycox, `[deep]` extra): discretized-time competing-risks net, per-cause CIF + concordance, `train-competing --deep-hit`. Gated tests skip without torch (live run needs the extra, like DeepSurv).
  - **Reverse-engineering rounds (6×, the mandatory self-audit).** (1) bootstrap **7m37s→2m50s** (the real cost was the per-resample concordance on 100k rows, not the redundancy); (2) cure correctness (expit/AIC-unpenalized) + the recalibration exploration above; (3) **caught a real NaN regression I'd introduced** in the EPSS memory-rewrite (valid-epss/NaN-percentile leaked NaN; the in-wild subset hid it) + a loud NaN guard; (4) adversarial-review findings (deephit contiguous-cause guard, narrowed except, bootstrap event-cap, fuller NaN guard); (5) **fetch robustness** — every HTTP connector had no timeout (hangs), + NVD 503/429 retry+backoff; (6) git subprocess timeouts (clone/pickaxe can hang) + edge guards. 209 tests green.
- **Laptop memory optimization (2026-06-19).** Audited the build/load paths for unnecessary loads and duplicate residency (measured, not guessed). **(1) EPSS scan 10×:** `build_epss_at_publication` / `build_epss_at_landmark` peaked ~5.8 GB RSS — root cause was the pyarrow `isin(338k cve_ids)` pushdown filter on the 375M-row file retaining ~8 MB/row-group (RSS climbed *linearly* though live state is one value per CVE). Switched to `ParquetFile.iter_batches` (no row filter) with CVE membership in numpy via a **fixed-size per-CVE reduction** — **565 MB peak**, output provably identical (338,015/4,650; 0 mismatches vs a direct reference on 200 real CVEs), ~same speed. **(2) Column projection:** `load_parquet(..., columns=...)` (via `schema_arrow` so list columns survive); the build loads only the 8 corpus columns it uses, leaving the 112 MB `description` text off the heap (274→152 MB). **(3) Write+free:** `build_dataset` writes per_signal/competing/in_wild and drops them before the EPSS phase. **Net: the full EPSS+landmark build that breached 8.5 GB now peaks 1.34 GB process RSS (~3.3 GB total)** — comfortably within the ≤6–8 GB budget. (Checked but reverted: a one-concat feature build — pandas-3 copy-on-write already makes the per-column adds cheap, so it measured *worse*.) 195 tests green.
  - **EPSS-enriched cure run (now unblocked by the memory fix).** Re-ran in-wild `cox,xgb,cure` on the 71-feature EPSS set (peak 1.34 GB). Honest nuance vs the lean-feature headline: cure stays the **best-calibrated** model on both sets (IPA ≥ cox, ≫ xgb), but the magnitude is feature-dependent — with EPSS its IPA sits **at** the null (30/90/180d: −0.0003/−0.0007/+0.0000) vs cox −0.0058/−0.0030/−0.0012 and xgb strongly negative; on the lean 68-feature set cure was clearly positive (+0.004/+0.005/+0.006). Discrimination matched (cure c-index 0.838 [0.795,0.881] vs cox 0.842 [0.799,0.885]). Takeaway: the cure model is the right tool for honest in-wild absolute probabilities; "beats the null" is clearest without the strong EPSS feature.
- **Finish-and-improve wave (2026-06-19 — spec `docs/superpowers/specs/2026-06-19-finish-and-improve-design.md`, plan `docs/superpowers/plans/2026-06-19-finish-and-improve.md`).** Four sub-projects, each TDD'd + committed + pushed.
  - **Live-fetch all sources + merge completeness.** Refreshed every reachable documented source to today: CISA KEV 1,623 (+81 vs handover), Google 0-day 404, **Exploit-DB 25,025 CVEs (new)**, PoC 168,739 (Trickest+Nomi-sec), Nuclei 4,208, EPSS daily snapshot; Metasploit live-mined (the long-open backlog item). NVD blocked by service-side 503 (latent connector bug *fixed*: bare dates → ISO-8601 extended); VulnCheck KEV blocked (no API token — wiring built+tested). `merge.py` gained specs for exploitdb/vulncheck_kev (live-only dedup) and the git-mined sources poc/nuclei/metasploit (were silently shadowed by the stale handover copy — now union-keep-earliest).
  - **In-wild label broadening + Exploit-DB tooling source.** `vulncheck_kev` → `IN_WILD_SOURCES` (in-wild), `exploitdb` → `EVENT_SOURCES` (tooling/first-weap, **+23,600 first-weap events**, PoC dominance 97%→84.5%). Hard-coded `KEV_CATALOG_START` generalized to a per-source `CATALOG_START` lookup + `in_wild_clock_start()` (max over active catalog sources, Google 0-day exempt, cutoff-collision guard). Rebuild: in-wild 1,543→1,582.
  - **Mixture-cure model (`cure.py`) — the headline contribution.** Logistic incidence + Weibull latency, MLE via scipy L-BFGS-B with an **analytic gradient** (finite-difference-checked); population `S(t)=(1-p)+p·S_u` plateaus at the cured mass. Wired as `--models cure`. **Result (in-wild, cutoff 2024-01-01, 68 leakage-safe features, no-EPSS this run for the memory budget): cure is the only model with positive IPA at 30/90/180d (cure +0.0036/+0.0050/+0.0057 vs cox −0.0043/−0.0013/+0.0004, xgb all negative), at matched discrimination (cure c-index 0.832 [0.788,0.876] vs cox 0.837 [0.793,0.88], overlapping CIs; cure has the best horizon-AUCs, 0.86@30d).** Fixes the audit's "IPA≈0" finding — honest absolute probabilities where Cox/XGB don't beat the base-rate null. **⚠️ OVERTURNED 2026-06-19 — see the rolling-origin backtest entry at the top: this single-split IPA win did not survive prospective walk-forward (cure went actively *negative*, IPA@180 down to −0.27 with recalibration). It was an identifiability artifact — a mixture-cure fraction is only identifiable with a Kaplan-Meier plateau, and our ~99.5%-censored in-wild data is administratively censored, not a cured population (Li, Taylor, Sy 2001). Cox is the in-wild backbone; cure is retained only as a documented dead-end.**
  - **CIF-based headline eval.** `cif_vs_independent` (unbiased Aalen-Johansen CIF beside the inflated naive 1-KM + inflation gap) and `cause_specific_cindex` (held-out per-cause Harrell C, None when undersupported) folded into `train-competing`'s `competing_metrics.json`. Real run: per-cause test c-index Metasploit 0.87 / KEV 0.78 / Exploit-DB 0.71 / PoC 0.56; google_0day → None (1 test event); inflation positive everywhere (e.g. PoC@180d AJ 0.126 vs naive 0.128).
  - **Memory discipline:** hard ≤6–8 GB RAM / ≤7 GB VRAM enforced throughout (`free -g` gating, peak-tracking watchers). The full EPSS-at-publication build peaks ~8.5 GB with landmarks (two 5.8 GB pyarrow scans) → exceeded budget and was aborted; the no-EPSS build (3.3 GB) and all training/competing runs (3.4–3.6 GB) stayed within budget. **Deferred:** an EPSS-enriched + landmark in-wild run (the EPSS scan alone needs ~5.8 GB RSS — run it at minimal baseline) and the VulnCheck/NVD live pulls.
- **Statistical-validity wave (2026-06-12, audit P0/P1 — see `docs/audit_2026-06-12.md`).** Labels: same-day events get 0.5d instead of being silently dropped (was 11% of first-weap / 26% of in-wild events); post-snapshot events censored at snapshot (fixes tau 855→801). Evaluation: Noether c-index SE/CI95 + event counts; censoring-free per-horizon AUC on the fully-observed subcohort (replaces trust in the degenerate IPCW weights); IPA (scaled Brier vs train-KM null). Cox: penalizer scaled by event rate (undoes ~24× over-shrinkage on in-wild) **with tenfold escalation on non-convergence** — the rescale alone let a quasi-separated covariate invert the landmark ranking (c-index 0.19, caught by ConvergenceWarning); `stable()` now requires ≥5 positives among events; PH screen keeps all events when sampling. `train` on in-wild auto-filters `published >= 2021-11-03` (KEV catalog launch; 25% of events were backfill-dated). **Honest headline numbers: first-weap xgb 0.598 [0.593,0.602], cox 0.565; in-wild static cox 0.849 [0.805,0.893]; in-wild landmark L=30 cox 0.873 [0.810,0.936] (AUC@90d 0.88). In-wild IPA ≈ 0/negative — ranking good, absolute probabilities don't beat the base rate (cure fraction).** 178 tests green.
- **Landmark features + edge-case audit wave (2026-06-12).** `landmark.py` implements research-roadmap P2: `build_landmark_features` (PoC/Metasploit/Nuclei presence/lag/count as-of `published+L`), `build_epss_at_landmark` (LAST EPSS reading in `[published, published+L]`, streamed like the publication scan), `restart_clock` (survivor risk set at L, clock shifted). CLI: `build-dataset --landmarks 7,30` writes `landmark_features_{L}d.parquet`; `train --landmark L` merges + restarts the clock (`metrics.json` records `landmark_days`). **In-wild L=30 ablation on the same risk set: cox c-index 0.819→0.874 (+5.5), xgb 0.785→0.798** (report: `artifacts/report_inwild_lm30/`; CI caveat — only ~190 test events, see audit). Two parallel audits (code edge cases + statistical methodology) ran the same day: six edge cases fixed immediately (lag-sentinel collision with pre-pub PoCs, duplicate-cve_id crash, NaT silently zeroing features, negative landmark, stale-parquet NaN merge that xgb silently trained on, xgb AFT inf risk scores on censored frames — all test-guarded); the statistical findings (silent duration-0 event drops = 26% of in-wild events, post-snapshot events unclipped, no CIs, IPCW degeneracy, 24× over-penalized in-wild Cox, KEV backfill clock) are the **next wave**, prioritized in `docs/audit_2026-06-12.md`. 169 tests green.
- **Feature-set rebuild + xgb early-stopping fix (2026-06-12).** The carried-over `artifacts/` predated the EPSS/CWE/CVSS-vector feature commits (27 cols, `epss_features_enabled: false`); rebuilt with `--epss-path` + `--technique-chain` (72 cols) — Cox c-index 0.549→0.588, integrated Brier 0.192→0.186. Separately, the `522ecfe` xgb early-stopping default was a regression: its time-tail validation split is mostly censored (shortest follow-up before the cutoff), so aft-nloglik rewards underfitting — stopped at iter 57/500 and cost 7 points (0.537 vs 0.607 without). `fit_xgb_aft` default is now `early_stopping_rounds=None` (opt-in, test-guarded). **Headline (first-weaponization, cutoff 2024-01-01, enriched features): xgb 0.607, cox 0.588.** Note 23/69 covariates violate PH — treat Cox as the interpretable reference, xgb-AFT as the headline model. Metrics: `artifacts/report_enriched/`.
- **WSL2 Ubuntu migration (2026-06-12).** Repo now lives at `/home/akim/Coding/Expl` in WSL2; env managed with **uv** (user preference). 155 tests green in 9.4 s; OneDrive pytest workaround (`--basetemp`/`no:cacheprovider` addopts) removed. Benchmarks vs Windows: full-corpus EPSS build **203 s vs 461.5 s (2.3×)** with identical output (338,015 rows / 4,650 missing / mean 0.02078, peak RSS 5.8 GB); `train --models cox,xgb` **36 s** (cox c-index 0.549 matches; the 1074 s Windows baseline predated sampled PH diagnostics, so timing isn't apples-to-apples; xgb 0.542 vs 0.587 — early-stopping commit landed between runs). GPU confirmed (`cuda:0`, RTX 4060). ⚠️ WSL RAM is ~7 GB, below the planned 12 GB cap — bump `.wslconfig` before RSF. Details: `docs/superpowers/plans/2026-06-12-wsl-migration.md`.
- **Laptop-memory program (2026-06-12).** Root cause of the `train` OOM: RSF evaluation materialized an (n_test × n_event_times) ≈ 87,616×5,627 float64 matrix (~3.9 GB) five times (eval + once per calibration horizon). Fixes: `modeling.survival_at` batches RSF predictions (2,048 rows) with a vectorized step-function lookup at the horizons only; one survival pass per model is shared between `evaluate_survival` (`surv_at_horizons`) and all `calibration_table`s (which now take precomputed `pred_event`). `build_epss_at_publication` streams the 375M-row EPSS file in 256k-row batches with capped readahead (`batch_readahead=2`) and an incremental per-CVE reduce — full-corpus EPSS now runs on a 16 GB laptop (the old `to_table().to_pandas()` materialized ~the whole file). `evaluate_deepsurv` caps at 20k sampled rows (its surv DataFrame is n_train_times × n_test). Verified: full `train` completes end-to-end on the real dataset (first time since the crash); `metrics.json` written.
- **GPU + model selection (2026-06-12).** New `xgb.py`: XGBoost AFT survival model (`[xgb]` extra) — trains on CUDA when present (verified `cuda:0` on the RTX 4060), closed-form survival curves so evaluation memory is flat; wired into `survival_at`/`evaluate_survival` as `kind="xgb"`. `train --models cox,rsf,xgb` selects models (default `cox,rsf` unchanged; `cox,xgb` is the laptop-friendly combo). `train --deep` fits/evaluates DeepSurv inline. RSF: `n_jobs=-1`, `min_samples_leaf=15` (fully-grown trees overfit censored data and balloon the per-leaf survival arrays). Real-data run: xgb IPCW c-index 0.587 vs cox 0.549 (first-weaponization, cutoff 2024-01-01).
- **Top-k CWE one-hot features** (`cwe_*`, default k=20) in `build_publication_features`, with provenance row. Stage logging (`logging.info`) through `train_command`; README gained a "check RAM/VRAM before model work" section.

- **Modeling & training (E):** `modeling.py` — `prepare_modeling_frame` (drops negative/zero durations), `time_split_frame`, leakage-safe numeric feature matrix, `fit_cox` (lifelines, penalized) + `fit_rsf` (scikit-survival RandomSurvivalForest, sub-sampled), and `evaluate_survival` with IPCW concordance + (integrated) Brier, horizon support-clipping, and tau fallback. `train` CLI subcommand fits both models on a time split and writes `metrics.json` (Cox, RSF, KM reference, naive event-rate). scikit-survival added as a dependency.
- Project `CLAUDE.md`; this `progress.md` roadmap.
- Per-signal labels + competing-risks labels (workstream A1/A2) — decouples PoC from in-wild signals.
- ATT&CK-chain features (B1) and EPSS-at-publication features (B2), both leakage-safe with provenance.
- **`vrs_presence` wired (B4)** — the last unused handover source — as a separate, leakage-flagged `presence_snapshot.parquet` (never merged into the safe features). **All nine handover sources are now used in the dataset.**
- **Fetch layer (C):** `Connector` interface + live connectors for **CISA KEV, EPSS-daily, NVD 2.0** + a `temporal-exploit fetch` CLI subcommand. Live KEV fetch verified against CISA — pulled 1,618 entries dated to yesterday (handover had 1,542).
- CLI `build-dataset` writes `per_signal_labels.parquet`, `competing_risks_labels.parquet`, `presence_snapshot.parquet`; optionally enriches features via `--technique-chain` / `--epss-path`; manifest records which sources were wired.
- Full real build (all nine sources) validated: corpus 338,015; presence 9,009; ATT&CK coverage 25.1%; presence flags confirmed OUT of the safe features; provenance carries both `publication_time_safe` and `snapshot_leakage`.
- Gap fixes from review: empty-frame concat FutureWarning eliminated (labels + nvd); EPSS earliest-row NaN-safe; NVD paging hardened against short/zero pages; NVD `cisa_exploit_added` + real `apiKey` header.
- 82 tests passing (`-W error::FutureWarning`).

## Done

### Modeling core (merged to `master`)
- **Package + tooling** — `pyproject.toml`, `src/temporal_exploit/` package, console script, pytest config.
- **Loaders + schema** — `load_parquet`, `load_required_parquets`, `validate_columns` with real handover column names. Fails loudly on missing columns.
- **Labels** — `build_first_weaponization_labels`: earliest event across the five dated sources, UTC-normalized, right-censoring at snapshot, `negative_duration_flag` (preserved not dropped).
- **Features** — `build_publication_features`: leakage-safe publication-time metadata (CVSS + missing indicator, severity one-hot, CWE/vendor/product counts, year). `feature_provenance()` audit trail. ndarray-safe list handling.
- **Splits** — time-based on `published`, locked `train/test_cve_ids.txt` + metadata. UTF-8.
- **Baselines** — Kaplan-Meier + Cox PH (lifelines), both reject negative durations.
- **Evaluation** — `event_rate_by_horizon` (7/30/90/180), `event_source_counts`.
- **CLI** — `build-dataset` builds labels + features + manifest, optional locked splits, always writes `feature_provenance.csv`.
- **Docs** — `docs/modeling_methodology.md` (verified accurate), README quick start, project `CLAUDE.md`.
- **Quality** — 34 tests; every phase passed two-stage review; validation gate run on the real 338k-row dataset (CVSS mean 4.98, non-zero list counts, PoC-dominant events).

### Critical defects fixed (found in first review)
- tz-aware date crash in label builder.
- wrong schema column names (`dateAdded`→`kev_date_added`, `date_discovered`→`zeroday_date_discovered`).
- wrong feature columns (`cvss_v3_base_score`→`cvss_v3_base`, `weaknesses`→`cwe_ids`) with silent zero-degradation.
- `list_len` returning 0 for numpy arrays.

## Data sources — wiring status

| Source | Parquet | Used today | Target |
|---|---|---|---|
| CVE corpus (NVD/VulnCheck) | `cve_corpus` | ✅ features + clock origin | keep |
| PoC (Trickest + Nomi-sec) | `poc_dates` | ✅ event source | split out as its own competing risk |
| Metasploit | `metasploit_dates` | ✅ event source | per-signal target |
| Nuclei | `nuclei_dates` | ✅ event source | per-signal target |
| CISA KEV | `kev_events` | ✅ event source | per-signal target + secondary in-wild label |
| Google 0-day | `google_0day` | ✅ event source | per-signal target + secondary in-wild label |
| EPSS history | `epss_history` | ❌ unused | EPSS-at-publication feature (leakage-safe) + reconciliation |
| ATT&CK chain | `technique_cwe_chain` | ❌ unused | tactic-level one-hot features + `has_attack_chain_mapping` |
| VRS presence | `vrs_presence` | ❌ unused | descriptive / censored-rows-only (snapshot leakage) |

## In progress / next (this initiative)

Four workstreams from the directive "more public datasets, fetch till now, deal with PoC timing, wire every source." Ordered by value/risk.

### A. Deal with PoC timing (highest priority — it's the core framing problem)
PoC is 97% of events, so a single first-weaponization model mostly learns PoC/disclosure logistics.
- **A1. ✅ Done.** `build_per_signal_labels` — per-source `{src}_event_date/_observed/_duration_days/_negative_duration_flag`, so each signal is modeled independently. Wired into the CLI (`per_signal_labels.parquet`). Real build: PoC observed 164,761; KEV 1,542; each signal now separable.
- **A2. ✅ Done.** `build_competing_risks_labels` — long-format `(cve_id, published, duration_days, event_cause, cause_code, event_observed)` for lifelines cause-specific hazards / pycox DeepHit. Deterministic `cause_code` (0==censored). Wired into the CLI (`competing_risks_labels.parquet`).
- **A3. ✅ Done.** `build_in_wild_labels` — secondary survival target using only KEV + Google-0day (PoC excluded). Wired into the CLI (`in_wild_labels.parquet`, manifest `in_wild_observed`). Real build: 1,543 confirmed in-wild events (KEV 1,410 + 0-day 133) vs 336,472 censored.
- **A4. ✅ Done.** `evaluate.cascade_order_stats` — for adjacent stages (PoC→MSF→Nuclei→KEV), % where stage a precedes stage b among CVEs observed in both, to motivate multi-state vs independent modeling.

### B. Wire every remaining source
- **B1. ✅ Done.** ATT&CK-chain features from `technique_cwe_chain` (`attack_features.py`): `has_attack_chain_mapping`, `attack_technique_count`, top-k parent-technique one-hot (parent = technique id with sub-technique stripped — lower dimensionality, no external tactic data needed). Wired into the CLI via `--technique-chain`. Real build: coverage 25.1% (matches MITRE-chain coverage). *Tactic-level aggregation deferred — needs the external ATT&CK technique→tactic map (a fetch, workstream D).*
- **B2. ✅ Done.** EPSS-at-publication (`epss_features.py`): first EPSS reading on/after each CVE's `published` (leakage-safe), `epss_at_publication_missing` for pre-2021-04-14/absent CVEs. pyarrow predicate pushdown over the 375M-row file. Wired into the CLI via `--epss-path`. Verified against the real 3.9GB file (sane values, ~9s for a small corpus). *Full-corpus EPSS is a heavier run; treat as an opt-in enrichment.*
- **B3. ✅ Done.** `evaluate.epss_reconciliation` — 2×2 of high-EPSS-at-publication × fast-weaponized (drops EPSS-missing CVEs), for RQ4 (where EPSS over/under-estimates observed weaponization).
- **B4. ✅ Done.** `vrs_presence` wired as the separate leakage-flagged `presence_snapshot.parquet` (see "Recently landed").

### C. Fetch data "till now" (live/incremental)
`src/temporal_exploit/fetch/` package: pluggable connectors that refresh each source to the current date and write to a separate `data/live/` dir (gitignored) without mutating the handover parquets, recording fetch provenance in `fetch_manifest.json`.
- **C1. ✅ Done.** `Connector` ABC + `save` + `write_fetch_manifest`. Network isolated in module-level `_fetch_*` fns (mocked in tests).
- **C2. ✅ Done.** CISA KEV live (`fetch/kev.py`). Verified live (1,618 rows to yesterday).
- **C3. ✅ Done.** FIRST.org EPSS daily CSV.gz (`fetch/epss.py`) → epss_history schema for a given day.
- **C4. ✅ Done.** NVD 2.0 API (`fetch/nvd.py`) → full cve_corpus schema, paged, apiKey header, hardened termination.
- **C5. ◐ Partial.** Git-mined sources, re-implemented in-package (not importing the immutable `enrich/` tree). Shared `fetch/gitmine.py` primitives: `shallow_clone` (`--no-checkout`, optional `with_blobs`), `first_add_dates` walk, `earliest_by_cve`, `file_at_head`, `earliest_introduction` (`-G` pickaxe). Connectors: **Nuclei** + **PoC** (Trickest+Nomi-sec) via path-based first-add; **Metasploit** (`fetch/metasploit.py`) via manifest + `git log -G` introduction date. **Project Zero** (`fetch/zeroday.py`) fetches Google's "0day In the Wild" sheet CSV export → `google_0day` schema. All wired into `fetch --source {nuclei,poc,metasploit,zeroday} [--repo|--url]`. Live-verified end-to-end: KEV/EPSS/NVD/Nuclei/PoC/Project Zero all fetch current-to-today (Nuclei 4,192 CVEs/13.7s; Trickest 159,902/19.3s; Nomi-sec 27,847/8.7s; Project Zero 404 rows/1.0s). Metasploit connector is mock-tested; its full live mine (blob clone + ~1hr `-G` across ~3,500 pairs) is opt-in and not run inline. **All README sources now have live connectors — section C complete.**
- **C5 note.** `merge.py` also reconciles a refreshed `google_0day` (full-snapshot, live wins), so the Project Zero connector flows through `merge` like KEV/EPSS/NVD.
- **C6. ✅ Done.** `merge.py` + `merge` CLI: `merge_live(handover_dir, live_dir, out_dir)` reconciles live deltas onto the handover parquets into a unified dir the build can consume — KEV full-snapshot (earliest `kev_date_added`), NVD corpus by newest `last_modified`, EPSS day-append by `(cve_id, date)`. Unmerged sources file-copy through (the 375M-row EPSS handover is never loaded unless it is the source being merged). Records per-source row deltas in `merge_manifest.json`.

### E. Modeling & training
- **E1. ✅ Done.** `modeling.py` + `train` CLI: Cox PH and RandomSurvivalForest on a time split, IPCW c-index and (integrated) Brier with horizon support-clipping, `metrics.json` report.
- **E2. ✅ Done.** `train --label-set {first_weaponization,in_wild}` selects the labels parquet; `metrics.json` records `label_set`. In-wild target (`in_wild_labels.parquet`, KEV+0-day only) is now trainable — the project's stated meaningful target.

### D. More publicly available datasets — DESCOPED
Per scope guidance (2026-06-12): stay within the README's defined sources. New external feeds (OSV, GHSA, ExploitDB, VulnCheck API) are **out of the project's main scope** and parked unless explicitly requested. The "more public data" need is met by live-refreshing the README's own sources (workstream C).

## Scope for improvement (backlog / quality)
- **Research-driven roadmap (2026-06-12)** — see `docs/research_improvements_2026-06.md` (adversarially verified literature sweep). **First implementation wave landed same day** (plan: `docs/superpowers/plans/2026-06-12-competing-risks-and-artifact-features.md`, four parallel subagents on disjoint files):
  - `competing.py` — Aalen-Johansen CIF (joint, unbiased), cause-specific Cox per transition, clock-restarted `transition_frame` (PoC→Metasploit/Nuclei/KEV), competing-aware `cif_calibration_table`.
  - `survboost.py` — SurvivalBoost (hazardous 0.1.0, `[boost]` extra) joint competing-risks CIFs; version-guarded sklearn≥1.6 compat patch.
  - `poc_features.py` — PoC artifact features (count/sources/lag/extension one-hots), provenance `transition_safe_post_poc` (label leakage for the PoC endpoint itself).
  - `fetch/exploitdb.py` + `fetch/vulncheck.py` — Exploit-DB verified flags and VulnCheck KEV connectors, wired into `fetch --source {exploitdb,vulncheck_kev}`.
  - Still open from the roadmap: landmarked time-varying design over transitions, CIF-based train-CLI integration (a `train-competing` command), label-noise/FFC adaptation, honeypot feeds.
- ✅ Artifact content hashes (`artifact_sha256`, SHA-256 per parquet/csv) in `manifest.json` for reproducibility.
- ✅ Cox proportional-hazards diagnostics — `modeling.cox_ph_assumptions` (per-covariate `proportional_hazard_test`, `violates` flag); folded into `train` `metrics.json` as `cox_ph_assumptions`.
- Event-source dominance warning emitted at build time when one source >X% of events.
- ✅ Calibration / reliability at 7/30/90/180 days — `modeling.calibration_table` (censoring-aware Kaplan-Meier observed rate per predicted-risk bin) + `plot_calibration`; `train` writes `calibration_{cox,rsf}.png` and folds the per-horizon tables into `metrics.json`. Constant-prediction (pre-first-event) bins collapse safely. (Brier already in `evaluate_survival`.)
- ✅ Description-text leakage mitigation (`text_safety.py`): `mask_leakage_terms` (redacts "actively exploited"/KEV/CISA/etc.) + `description_is_fresh` / `build_safe_descriptions` (blanks descriptions back-edited > ε days after `published`). Groundwork utility — not wired into the build until an NLP feature consumes it.
- ✅ Deep survival model (DeepSurv via pycox) — `deep.py` (`fit_deepsurv` + `evaluate_deepsurv` with time-dependent concordance + integrated Brier). Auto-selects CUDA when available (moves the net to GPU). Kept optional: lazy torch imports, `deep` extra (`pip install -e ".[deep]"`), `importorskip`-gated tests so core/CI stay torch-free. Core suite stays green (deep test skips without torch). Verified end-to-end on GPU in an isolated torch(cu128, py3.14)+pycox venv — trained on the RTX 4060, evaluated with time-dependent concordance + integrated Brier. Needed a `scipy.integrate.simps`→`simpson` compat shim (pycox<=0.3 vs scipy>=1.14). Now wired into the CLI as `train --deep`; evaluation sampled at 20k rows for memory.
- ✅ CI workflow (`.github/workflows/ci.yml`: ruff + pytest on push/PR, py3.12) and pre-commit hooks (`.pre-commit-config.yaml`: hygiene hooks + ruff). Ruff lint is pyflakes-only (`F`) — real defects, no reformatting churn.
- ✅ Pytest workaround baked into `pyproject` `addopts` (`--basetemp=.pytmp -p no:cacheprovider`) + `filterwarnings=["error::FutureWarning"]`, so plain `pytest` runs with the gate on.
- ✅ EPSS scan date-pushdown (`epss_features._iter_epss_batches` / `_epss_row_groups`) — the 375M-row history is 1 row group per day, so both readers now skip groups outside `[earliest published, snapshot]` (provably result-preserving: skipped rows are exactly those the per-CVE `elig` mask discards). Speeds historical-snapshot/backtest builds; no-op for current-snapshot; memory unchanged (~300 MB). DRY'd the duplicated scan loop out of `epss_features`/`landmark`.
- ✅ In-wild head-to-head uncertainty — `backtest.paired_origin_deltas` pairs two rolling-origin backtests on their **shared** origins → mean delta + 95% CI + win-fraction (the honest paired test vs comparing two independent across-origin sds). `scripts/inwild_method_headtohead.py` writes a `paired_vs_cox` block + a "within error / beats cox" verdict. Regenerated `inwild_headtohead.json`: gbm vs cox AUC@90 = −0.108, CI [−0.154, −0.061] (reliably worse, CI excludes 0); cox AUC@90 0.817±0.069 reconfirms the in-wild margin is data-limited, not model-limited.
- ✅ Research accuracy pass (`docs/research_competitive_methods_2026-06.md`) — corrected the stale EE figure to the web-verified Suciu USENIX'22 headline (86% vs 49% precision, PR-AUC ≈0.90) and the false "we already clone the PoC repos" claim (`fetch/poc.py` clones the *index* repos with `blob:none` → `poc_path` strings only, not exploit code). Companion `docs/poc_content_features_plan.md` scopes the EE PoC-content lift, leakage-gated to the transition/landmark heads (PoC-content is leaky on the published→PoC clock).
- ✅ **Session audit (32-agent workflow) + fixes — RE of every function written this session + epss_history data-completeness.** 24/26 findings confirmed; all fixes TDD'd, GPU-only, full suite 268✓/3-skip:
  - **HIGH** the in-wild experiment scripts ran on the stale ~454-event set + defaulted to stashed CPU models → `inwild_method_headtohead.py`/`inwild_epss_ablation.py` now load in-wild labels from `data/live` (VulnCheck, 1368 events), default to `[xgb,cox]` (GPU AFT; cox = paired reference), report PR-AUC, print loaded/missing sources, and assert the EPSS-only baseline is the static publication FLOOR (commits `aa7f200`, `fa3f615`).
  - **MED** epss_history under-exploited at the landmark → `build_epss_at_landmark` now also emits `epss_mean/std/reading_count_to_landmark` + `days_to_epss_{01,05}` via free O(1) streaming accumulators (no extra file scan), all landmark-safe + provenanced (`1bb79d2`).
  - **MED** IPCW time-dependent `cumulative_dynamic_auc` was installed-but-unused → added as a single-split cross-check (2D `1-surv` estimate, try/except); `horizon_auc` stays primary (`7ac5bf4`). PR-AUC/average-precision added to the eval panel + backtest aggregate earlier (`1003a6e`).
  - **Data-discipline**: `load_optional_event` projects event parquets to needed columns; `sources_for_label_set` makes in-wild backtests skip poc's 188k rows (+exploitdb 30k); ablation iterates configs one at a time. Dup-check in `build_epss_at_publication`, defensive `_aggregate` `.get`, docstring tightenings (`808fdd2`).
- ✅ **F5 EPSS scan fusion** — `build_epss_features` (landmark.py) decodes the 375M-row file ONCE for publication + every landmark window (extracted `_pub_reduce_*` / `_landmark_reduce_*` reducers; equivalence test proves byte-identical output to the separate builders and locks the pre-2021 trap — publication is never derived from landmark). cli build wired to it: 1 scan instead of 1+N. Commits `9d855ef`,`b31799a`,`fdc00b3`,`1c77671`.
- ✅ **F6 landmark-aware circularity control** — `rolling_origin_backtest(landmark_days=L)` applies `restart_clock` to train+final labels per origin (`f181964`); `scripts/inwild_epss_ablation_landmark.py` (`0ad2c41`) builds the landmark trajectory EPSS, merges it, and runs full/no_epss/epss_only in the landmark regime so the EPSS-only baseline is the STRONG trajectory EPSS (~0.63–0.68), making `full vs EPSS-only` the real bar against distillation.

- ✅ **Source-usage audit + attacker-incentive features.** Audited every dataset: the 71-col matrix used only corpus (CVSS/CWE/CPE) + epss_history + technique_cwe_chain. Added 9 publication-time attacker-incentive flags from `cvss_v3_vector` (wormable = AV:N+PR:N+UI:N+AC:L, unauth-network-high-impact, scope-changed, ...) via `incentive_features.py`, wired always-on into the build (`4c66a11`). **exploitdb** now evaluated (N6 below — near-empty as a transition target, best used as PoC label enrichment); **vrs_presence** excluded by leakage design; **shadowserver** has no global data.
- ✅ **Next-phase plan** (4-lens design + adversarial-judge review workflow) → a 6-task, F6-aware plan that steers AWAY from the EPSS-distilling in-wild head toward the instant/transition heads, exploitdb, and non-stationarity. Executed:
  - **N1** (`d37cf9a`) — investigated a review-flagged landmark-backtest leak and **proved it does NOT exist**: `restart_clock` drops as-of-origin duration ≤ L, so every train CVE has pub < t−L (its window is closed before the origin). **F6's EPSS-distillation result is sound, not a leakage artifact.** `embargo_days` kept as an optional purge gap; regression-tested.
  - **N2** (`c6e8a0e`) — `LABEL_BUILDERS['instant']` + `feature_view='publication_only'` (strips every `*_to_landmark`/`*_by_landmark`/`days_to_epss` col) = the deployable t=0 cold-start head.
  - **N3** (`9ceebf2`) — `scripts/operating_points.py`: instant L=0 vs landmark L=7,30 on the in-wild set. Result: **waiting after disclosure buys recall** — recall@top-decile **0.28 (L=0) → 0.34 (L=7) → 0.49 (L=30)**, PR-AUC rising, at the cost of cohort size (test events 1201 → 931). Quantifies the cold-start cost.
  - **N4** (`68271c0`,`a090e15`) — `incentive_feature_columns` + `scripts/instant_incentive_ablation.py`: instant-head (publication-only, no landmark) ablation full / no_incentive / epss_floor_only over short horizons (1/3/7/30). **Verdict:** the full structured model **beats the EPSS static floor** on first_weaponization (PR-AUC@7 +0.049 CI[+0.038,+0.059], @30 +0.064 CI[+0.043,+0.084]) — CVSS/CWE/CPE carry real t=0 signal — but the **incentive flags add ~0** on both heads at every short horizon (within error), extending F6's redundancy verdict to t=0. On in_wild even the full model ≈ EPSS floor.
  - **N5** (`340af87`,`e146816`,`d221fa7`) — `era_stress_eval` + `scripts/era_stress.py` (the P0 validity gate): train-old/test-new vs an in-period control (both trains as-of `train_max`, de-confounded per the audit) on first_weaponization@90. **Result is era-dependent, not a clean degradation:** train≤2022/test≥2024 = **−0.031** (cross-era AUC 0.555 vs in-period 0.587, n=34,921; the pre-fix −0.073 was ~half snapshot-censoring confound) but train≤2023/test≥2025 = **+0.074** (cross 0.615 > in-period 0.541) — the opposite sign. A **residual confound remains** (the in-period TEST has less follow-up than the cross-era test, so at the 90d horizon it is scored on an earlier within-window slice). **Honest verdict: on this signal-starved publication-only head (AUC 0.54–0.62) era-stress is directional/inconclusive — the "745d→44d" median-time collapse does NOT manifest as a clear, consistent ranking-AUC degradation.** A cleaner measurement needs follow-up-matched test windows (next-agent item).
  - **N6** (`bd99518`,`04cd587`,`d221fa7`) — `build_transition_labels` + `transition_cindex` + the registered `poc_to_exploitdb` head + `scripts/transition_poc_to_exploitdb.py` (verified_only filter, 24.7k verified rows). **Verdict: the PoC→verified-ExploitDB forward transition is near-empty (68 events / 162,730 PoC'd CVEs, 0.04%)** because **99.4% of verified-ExploitDB entries PRECEDE the aggregated PoC date** (ExploitDB is itself an early PoC source; only 0.5%/71 are genuine escalations). When it happens it is fast (median 31–49d) and ranks above chance pooled (AUC@90 ~0.74 / 46 test events) but PR-AUC ~0.007 at the 0.04% base rate, and a clean held-out split has just 2 events. **ExploitDB-verified is label enrichment, not a downstream target.** Transition machinery is reusable for PoC→Metasploit / PoC→KEV.
  - **Reverse-engineering audit** (`d221fa7`) — 5-agent adversarial review of N4–N6 (13 findings: 0 critical / 6 high). Fixed: `transition_cindex` rename (collision with `competing.cause_specific_cindex`) + all-censored guard; the era-stress in-period confound (now both trains as-of `train_max`); competing-source validation + label-based indexing in `build_transition_labels`; script double-loads (ExploitDB, corpus); explicit held-out transition c-index. **Known/deferred:** `rolling_origin_backtest` never extracts the per-origin IPCW c-index into the aggregate (pre-existing, affects all heads).

### 2026-06-20 — Data-expansion integration (VulnCheck + NVD++ merged → rebuilt → re-evaluated)

Executed the `2026-06-20-data-expansion-roadmap.md` + `2026-06-20-integrate-fetched-data.md` plans end-to-end on the staged live data (no re-fetch needed):

- **Merge** (`data/merged/`, `merge_manifest.json`) — folded the live deltas onto the handover: corpus **338,015 → 359,507** unique (NVD++ added ~21k CVEs), KEV 1,542→1,623, Google 0-day 344→404, plus `merge_live_only` sources VulnCheck KEV (4,969) / ExploitDB (30,323) / EPSS-history. No dup `cve_id`.
- **Rebuild** (`artifacts/merged/`, snapshot 2026-03-14, landmarks 7/30) — peak RSS **1.49 GB**, 2m35s (streamed EPSS path held; ~3 GB abort threshold never approached). **In-wild events 454 → 4,690** (>10×); first-weaponization 170,951 / 359,507; all 9 EPSS-landmark dynamics columns present, **0 NaN**.
- **Re-evaluate** (`scripts/inwild_merged_eval.py` → `artifacts/merged/inwild_epss_ablation.json`) — GPU xgb-AFT, 15 rolling origins (2022-01 … 2025-07), expanded in-wild labels, three feature configs on identical origins.

  **The PR-AUC-vs-EPSS-only bar is CLEARED on the expanded labels:** full vs static-EPSS-only PR-AUC@30 **+0.0089 CI[0.004, 0.014]** (win 0.93), PR-AUC@90 **+0.0134 CI[0.001, 0.026]** (win 0.93) — both CIs exclude 0. Ranking is dominant: AUC@30 **+0.233** (win 0.87), AUC@90 **+0.247** (win 1.00). Absolute: EPSS-only AUC@90 **0.505** (static pub-EPSS ≈ coin-flip on ranking) vs full **0.752** vs no-EPSS **0.818**.

  **But static publication-time EPSS as a *feature* hurts:** full vs no-EPSS AUC@30 **−0.074 CI[−0.121, −0.027]**, AUC@90 **−0.067 CI[−0.101, −0.032]** (win 0.20–0.27; PR-AUC −0.008/−0.012, CI straddles 0). **The deployable config is the no-EPSS structural model** (CVSS/CWE/CPE/ATT&CK). The two results together are the point: the model's win over EPSS-only comes **entirely from non-EPSS structural signal, not from distilling EPSS** — sharpening, not overturning, the data-limited / cold-start-structural thesis. (Caveat: this is *static* pub-time EPSS; the landmark EPSS-trajectory + `restart_clock` arm is untested here — integration-plan Task 4 Step 2 remains open.)

  **Honest framing unchanged:** even at 4,690 events absolute PR-AUC stays ~0.02–0.03 (≈1–2% in-wild prevalence — the rare-event regime). The model **ranks** in-wild risk well (AUC 0.75–0.82) and clears the EPSS-only bar, but precision is prevalence-bounded. More labels raised the floor and tightened CIs; they did not change the regime.

### 2026-06-21 — Landmark L=30 EPSS-trajectory + restart_clock arm (the STRONG circularity control — CORRECTS the F6 "distillation" verdict)

Ran the one remaining leakage-safe EPSS arm: does the in-wild model still beat an EPSS-only baseline once that baseline is upgraded from the *static* publication reading (~0.50 AUC) to the **full landmark trajectory** (velocity/max/mean/std/rising + days-to-threshold)? `scripts/inwild_merged_landmark_eval.py` (merged build) + a current-code re-run of `scripts/inwild_epss_ablation_landmark.py` (handover build) — `rolling_origin_backtest(landmark_days=30)` applies `restart_clock` (cohort 4,690→**2,693** events; the ~2k fast in-wild events within 30d are conditioned out), GPU xgb, 15 origins, full / no_epss / epss_only on identical origins+cohort.

- **Leak fixed first:** `days_to_epss_01/05` are EPSS-derived but escaped the `epss`-prefix filter and were sitting in the "no_epss" arm. Folded them into the EPSS baseline so no_epss is **truly EPSS-free** — required before any "structural carries it" claim.
- **Verdict (identical on handover + merged → robust, NOT data-driven):**

  | config | AUC@30 | AUC@90 | PR-AUC@90 |
  |---|---|---|---|
  | full (structural+EPSS) | 0.841 | 0.784 | 0.046 |
  | **no_epss (pure structural)** | 0.808 | **0.824** | 0.028 |
  | epss_only (complete EPSS: trajectory+timing) | 0.638 | 0.599 | 0.032 |

  **On RANKING (AUC — the powered metric at ~1% prevalence): pure structural features BEAT the complete EPSS baseline by +0.171 (AUC@30) / +0.225 (AUC@90).** full vs complete-EPSS-only AUC@90 **+0.185 CI[0.112, 0.258]** win 0.93. Adding EPSS to structural does **not** improve ranking (full 0.784 ≤ no_epss 0.824). **On PRECISION (PR-AUC@90 ~0.03–0.05): the configs are statistically indistinguishable** (CIs span the effect) — PR-AUC is underpowered at this rarity; the `days_to_epss` timing features carry most of the small PR-AUC signal.

- **This CORRECTS the prior F6 "largely distilling the EPSS trajectory" conclusion** (README + `docs/model_improvement_and_edge_cases_2026-06-20.md`). F6 judged on PR-AUC alone — where full ≈ EPSS-trajectory, but *uninformatively* (underpowered) — and never reported the decisive AUC gap. Re-running on **both** builds with current code gives the same answer, so it's a metric/measurement correction, not a data effect. **EPSS — even the full trajectory — is redundant given structural features for ranking in-wild risk; the deployable in-wild config should DROP EPSS** (no ranking help, adds mild noise). Consistent with the t=0 static result (no_epss best there too). Net across both regimes: the model's edge over EPSS is **structural CVE metadata, not EPSS distillation** — which strengthens the cold-start-structural thesis. **Methodological note:** at ~1% in-wild prevalence, **AUC is the powered metric for model separation; PR-AUC is the right deployment metric (absolute precision) but lacks power to compare configs** — report both, don't read a PR-AUC tie as evidence of equivalence.

### 2026-06-21 — Re-center on the multi-state pipeline (RQ2) + label-validity finding

Acting on the alignment check against `temporal_exploit_prediction.md` (the professor's spine is the multi-state PIPELINE + deep-at-scale, not the data-starved in-wild head). Phase 1 of `docs/superpowers/plans/2026-06-21-recenter-pipeline-and-deep-at-scale.md`. Full writeup: `docs/pipeline_characterization_2026-06.md`. **7-round reverse-engineering audit; one round forced a correction (§3 below).**

- **Label-validity finding (the headline).** The ordering check surfaced that the **git-mined PoC date conflates true PoC-publication with aggregator BULK-INDEX dates** for older CVEs (Feb-2022 spikes 15k/11k/8k CVEs; 2019-12-08 carries 1,151; EternalBlue/Heartbleed both mis-dated 2019-12-08). PoC-before-MSF is **31% over all CVEs but 81% for CVEs published ≥2022**, monotonically rising by publication year (56.7→66.9→76.1→81.0 while n shrinks) → publication-age-driven artifact, not sample size. Primary suspect for the weak first-weap AUC (0.60), since PoC is 97% of events. **Mitigation: restrict pipeline timing to the clean recent cohort (published ≥2021).** This is exactly the "look before you model" defect the doc says to catch pre-modeling.
- **Pipeline cascades (RQ2-a).** On the clean cohort the order PoC→MSF→Nuclei→KEV holds (PoC→MSF 81%, MSF→Nuclei 60%, Nuclei→KEV 64%).
- **Competing-risks dependence (RQ2-b) — CORRECTED by the RE audit.** Absolute CIF-vs-independent deviation is ~1e-3 everywhere (looks "negligible"), but **relative to the rare causes' small CIFs it is 13–18%** (MSF/Nuclei/KEV/VulnCheck/ExploitDB); only the dominant PoC cause is genuinely independence-like (0.75%). So a *joint* competing-risks model (AJ / cause-specific Cox / DeepHit) is materially justified for the rare transitions — motivates Phase 2's competing-risks deep arm.
- **Per-transition predictability (RQ2-c), recent cohort, held-out time-split.** PoC→MSF **near-chance** (cox 0.59 / xgb 0.53, 510 ev) — Rapid7's weaponization choice isn't in publication metadata; PoC→Nuclei **good** (xgb 0.79, 1,588 ev); **PoC→KEV strong** (cox 0.819 [0.775,0.864], xgb **0.869 [0.839,0.897]** bootstrap, 293 ev / 83 test) and fast (median 67 d). **The conditional "PoC-present → time-to-KEV" (0.87) is far stronger than the unconditional first-weap model (0.60)** — the deployable framing.

### 2026-06-21 — Phase 2: deep-survival-vs-Cox at first-weaponization scale (the AI artefact) + lossless RAM cut

Full writeup: `docs/deep_survival_headtohead_2026-06.md`. ≥8-round RE audit (one round fixed a metric bug, one caught a small-sample caveat). `[deep]` sidecar (torch 2.12+cu130, RTX 4060) via `uv --torch-backend=auto`.

- **Single-event (212k train / 101k test, 45,527 test events):** discrimination **xgb-AFT 0.614 [0.610,0.619] > DeepSurv 0.575 > Cox 0.562 [0.558,0.567]**; calibration (integrated Brier ↓) **Cox 0.220 < xgb 0.240 < DeepSurv 0.287**. **DeepSurv wins neither axis** (worst calibration). xgb wins discrimination, Cox wins calibration — textbook tabular-survival, matches Burk 2026.
- **Competing-risks:** cause-specific Cox is the **working** model — poc 0.563 [0.560,0.566] (45,104 test ev, **matches single-event Cox 0.562 — cross-check**), vulncheck_kev 0.749 (102 ev); metasploit 0.889 / kev 0.834 are high but on **36 / 6 test events** (small-sample, RE-flagged as not robust). **DeepHit COLLAPSED** — CIF@90 ≈ 3e-6 for poc vs AJ truth ~0.19 (5 orders too small), concordance ~0.5; a known extreme-imbalance failure mode (52% censored, poc 84% of events). A negative result, not a fair comparison.
- **Verdict:** at this scale **deep survival does not win on either head**; the working tools are classical (xgb-AFT discrimination, Cox calibration + interpretable competing-risks). Exactly the professor's "characterise where they win and lose."
- **Metric bug fixed:** `bootstrap_cindex_report` collapsed to an events-only subpopulation when events>max_eval (45k>20k), so its CI didn't bracket the ipcw point estimate; now retains a censored subsample (TDD regression). Headline uses the Noether CI.
- **Lossless RAM cut:** `downcast_int_features` shrinks the ~70 binary int64 flag columns to int8 (publication path 210.8→44.5 MB, competing 217.4→51.0 MB, ~4.5×) wired into `prepare_modeling_frame` + `prepare_competing_frame`. **Proven zero quality loss** (RE on real data): float64+float32 feature matrices bit-identical, cox & xgb risk identical to **0.00e+00**, deep models see identical float32 input. Floats (CVSS/EPSS) + meta/label columns untouched. Data integrity verified: no duplicate cve_id, train/test overlap = 0, 1:1 merge.

### 2026-06-21 — Completed the professor's remaining directions (RQ1 + RQ3 + defender interpretation)

Plan `2026-06-21-complete-prof-directions.md`. **Aggressive RE (user directive: ≥10 rounds + multi-seed + 1000-resample bootstraps + adversarial nulls per deliverable)** — it caught/fixed real bugs each phase.

- **Direction 1 — Time-to-PoC** (`scripts/time_to_poc_analysis.py`, `docs`-folded). Clean cohort (published≥2021): cox 0.594 / xgb 0.567, median 58d. **RE caught + fixed a dup-cve_id bug** (poc_dates has 19k CVEs with >1 source row → merge exploded; dedup to earliest PoC). **Decomposition (the honest answer): the c-index is INCIDENCE-dominated (~0.59 which-CVEs-get-a-PoC) not timing — among-events timing-only c-index 0.534 [0.528,0.540].** 20-perm full-null centered 0.503 → no leakage. EPSS+severity+CWE drive it; by-year median (1988→12,365d … 2022→92d) nails the PoC-date artifact. Matches the prof's "PoC timing is disclosure-logistics-driven."
- **Direction 3 — Tactic conditioning** (`scripts/tactic_conditioning.py`). **ATT&CK tactic significantly conditions weaponization speed:** Defense Evasion/Persistence fastest (~35–46d), Credential/Initial Access slowest (~63–72d); log-rank p≈1e-102. **RE: adversarial tactic-shuffle p=0.83 (signal real), de-overlap to 1 primary-tactic/cve p=1e-64 (ordering holds), mapped-vs-unmapped confound handled** (read within mapped — the prof's warning), bootstrap-stable, holds on first-weap labels. ~24% ATT&CK coverage caveat preserved.
- **Defender interpretation + state-aware triage score** (`scripts/defender_score.py`, `docs/defender_interpretation_2026-06.md` — the required §"what would a defender do differently" deliverable + a model improvement). **Honest finding (RE ruled out a bug): at publication EPSS-only beats the survival model at the sharp top (top-1% prec 0.468 vs 0.278) though the model wins overall c-index (0.585 vs 0.560)** — EPSS concentrates the top, survival wins the middle. **Deployable value-add is STATE 2: PoC→KEV escalation, recall@top-10% 0.515 [0.40,0.65], ~42d lead, no leakage, adversarial-shuffle 0.14.** Improvement = state-aware composite (EPSS at publication + tactic/incidence context → escalate to PoC→KEV on PoC-appearance). Complements EPSS, doesn't replace.
- **§"What success could look like" now fully covered:** defensible label construction ✓, survival models + time-split + calibration/discrimination ✓, framing treatment (PoC dominance + informative censoring) ✓, **interpretation connecting to vuln-mgmt practice ✓ (this deliverable)**; exceptional tier: multi-state ✓, EPSS reconciliation ✓ (downstream tool = optional Phase D, deferred). Directions 1–4 all addressed.

## Conventions
- TDD, one focused commit per change, two-stage review for non-trivial work.
- Leakage discipline and tz/column-name rules per `CLAUDE.md`.
- Generated data (`artifacts/`, `data/live/`) stays gitignored.
